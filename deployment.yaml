apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-a
spec:
  replicas: 2
  selector:
    matchLabels:
      app: microservice-a
  template:
    metadata:
      labels:
        app: microservice-a
    spec:
      containers:
        - name: microservice-a
          # IMPORTANT: This is a placeholder image.
          # The CI pipeline will replace this with the real image URL.
          image: "europe-west1-docker.pkg.dev/sm-sandbox-467916/app-images/microservice-a:e39a69e"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          ports:
            - containerPort: 8080
