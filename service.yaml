# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: microservice-a
spec:
  type: ClusterIP # Only reachable inside the cluster
  selector:
    app: microservice-a
  ports:
  - port: 80
    targetPort: 8080